import { ProjectSummary } from "@/types/project";
import { currencyConverterSummary } from "@/content/projects/currency-converter/metadata";

export const projects: ProjectSummary[] = [
  currencyConverterSummary,
];